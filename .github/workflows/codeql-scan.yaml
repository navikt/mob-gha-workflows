name: CodeQL Scan

on:
  workflow_call:
    inputs:
      language:
        required: true
        type: string
        description: "Programming language to analyze (eg: java or javascript)"
      project-directory:
        required: false
        type: string
        description: Root directory of the project to build, relative to repo root. Use for monorepos where the build is not at repo root. Only relevant for compiled languages (kotlin, java, java-kotlin).
        default: "."
      build-command:
        required: false
        type: string
        description: Build command to run for compiled languages when project-directory is not repo root. Ignored for languages that use autobuild (project-directory == ".") or that don't require a build (eg. javascript).
        default: "./gradlew build -x test --configuration-cache"

jobs:
  codeql-scan:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: ${{ inputs.language }}

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # ratchet:step-security/harden-runner@v2.21.0
        with:
          egress-policy: audit

      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # ratchet:actions/checkout@v7.0.1
        with:
          persist-credentials: false

      - name: Set up JDK
        if: ${{ contains(fromJSON('["kotlin","java-kotlin","java"]'), inputs.language) && inputs.project-directory != '.' }}
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c # ratchet:actions/setup-java@v6.0.0
        with:
          distribution: "temurin"
          java-version: "25"

      - name: Initialize CodeQL
        uses: github/codeql-action/init@db488ddef3bf6cb639b32c2e9a7c0a7ea8271d28  # ratchet:github/codeql-action/init@v4.37.8
        with:
          tools: linked
          languages: ${{ matrix.language }}
          build-mode: ${{ contains(fromJSON('["kotlin","java-kotlin","java"]'), inputs.language) && (inputs.project-directory == '.' && 'autobuild' || 'manual') || 'none' }}
          queries: security-and-quality

      - name: Build (monorepo project-directory)
        if: ${{ contains(fromJSON('["kotlin","java-kotlin","java"]'), inputs.language) && inputs.project-directory != '.' }}
        working-directory: ${{ inputs.project-directory }}
        env:
          BUILD_COMMAND: ${{ inputs.build-command }}
        run: $BUILD_COMMAND

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@db488ddef3bf6cb639b32c2e9a7c0a7ea8271d28  # ratchet:github/codeql-action/analyze@v4.37.8
        with:
          category: "/language:${{ matrix.language }}"
